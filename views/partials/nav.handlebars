<script>
  console.log('calling nav');
</script>

<div class="pure-u-1 pure-u-sm-1 pure-u-md-3-5 pure-u-lg-1-2">
  <nav>


   {{#unless loggedInUser}}
   <a href="/login">login</a>
    {{/unless}}
    {{#if loggedInUser}}
    <a href="/profile/{{loggedInUser.id}}">profile</a>
    <a href="/friends">friends</a>
    <a href="#" id="randomButton">random</a>
    <a href="/search">search</a>
    <a href="/logout">logout</a>
    {{/if}}

    
  </nav>
</div>

<script>

const randomUser = document.querySelector('#randomButton'); 
randomUser.addEventListener('click', (event => {

  console.log('hi');

  {{!-- const getUsers = async params => {
  const { count, rows: users } = await User.findAndCountAll({
    order: [['id', 'ASC']],
    ...params,
  });
  console.log(count);
  return { count, students }; --}}
}


    const randomNum = Math.ceil(Math.random() * 6);
    const splashURL = '../assets/images/splash0' + randomNum + '.jpg';
    // console.log(splashURL);
    document.getElementById("splashImage").src = splashURL;

}));

</script>